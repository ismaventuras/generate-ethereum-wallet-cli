Generate new ethereum-compatible wallets (mnemonic or private keys).

## Requirements

Node.js installed

## Usage

generate a new mnemonic
```npx generate-ethereum-wallet-cli -m```
generate a single private key
```npx generate-ethereum-wallet-cli -p```